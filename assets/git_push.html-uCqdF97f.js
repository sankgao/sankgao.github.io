import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as a,c as s,f as t}from"./app--yctzgvx.js";const d={},n=t(`<h2 id="描述" tabindex="-1"><a class="header-anchor" href="#描述" aria-hidden="true">#</a> 描述</h2><p><code>git push</code> 用于将本地分支的更改推送到远程仓库。默认情况下，它推送当前活动的本地分支到与其同名的远程分支。您也可以指定要推送的本地分支和远程分支，以及推送的内容（例如：标签或特定的提交）。</p><p><code>git push</code> 是协作开发中的重要命令，它允许团队成员共享代码更改并保持代码的同步。在使用它时，了解您的操作如何影响其他人以及如何在不造成冲突的情况下进行推送是非常重要的。</p><h2 id="选项" tabindex="-1"><a class="header-anchor" href="#选项" aria-hidden="true">#</a> 选项</h2><table><thead><tr><th style="text-align:center;">选项</th><th style="text-align:left;">描述</th></tr></thead><tbody><tr><td style="text-align:center;"><code>--all, --branches</code></td><td style="text-align:left;">推送所有分支到远程仓库</td></tr><tr><td style="text-align:center;"><code>--tags</code></td><td style="text-align:left;">推送所有标签到远程仓库</td></tr><tr><td style="text-align:center;"><code>--prune</code></td><td style="text-align:left;">删除远程仓库中已经不存在的本地分支</td></tr><tr><td style="text-align:center;"><code>--repo=&lt;repository&gt;</code></td><td style="text-align:left;">指定远程仓库</td></tr><tr><td style="text-align:center;"><code>-d, --delete</code></td><td style="text-align:left;">删除远程分支</td></tr><tr><td style="text-align:center;"><code>-f, --force</code></td><td style="text-align:left;">即使远程仓库中有冲突或更新，也强制推送更改</td></tr><tr><td style="text-align:center;"><code>-u, --set-upstream</code></td><td style="text-align:left;">指定默认远程仓库</td></tr><tr><td style="text-align:center;"><code>-n, --dry-run</code></td><td style="text-align:left;">查看将要推送的远程分支，但不实际推送</td></tr><tr><td style="text-align:center;"><code>--progress</code></td><td style="text-align:left;">显示推送进度</td></tr><tr><td style="text-align:center;"><code>--porcelain</code></td><td style="text-align:left;">输出一个简化的格式，通常用于脚本</td></tr><tr><td style="text-align:center;"><code>-v, --verbose</code></td><td style="text-align:left;">显示详细的输出</td></tr></tbody></table><h2 id="示例" tabindex="-1"><a class="header-anchor" href="#示例" aria-hidden="true">#</a> 示例</h2><h3 id="直接推送" tabindex="-1"><a class="header-anchor" href="#直接推送" aria-hidden="true">#</a> 直接推送</h3><p>当命令行没有用 <code>--repo=&lt;repository&gt;</code> 参数指定推送位置时，会参考当前分支的 <code>branch.*.remote</code> 配置来决定推送位置。如果配置丢失，则默认为 <code>origin</code>。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="推送当前分支" tabindex="-1"><a class="header-anchor" href="#推送当前分支" aria-hidden="true">#</a> 推送当前分支</h3><p>例如：推送名为 <code>main</code> 的本地分支到名为 <code>origin</code> 远程仓库的对应分支。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push origin main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li><code>origin</code> 是远程仓库的默认名称，通常用于标识您的远程仓库</li><li><code>main</code> 通常指的是您想要推送到的远程仓库的分支名</li></ul><p>如果您当前的本地分支名是 <code>main</code>，但远程仓库中没有名为 <code>main</code> 的分支，它将在远程仓库中被创建；如果您当前的本地分支名不是 <code>main</code>，而您想要推送到远程仓库的 <code>main</code> 分支，您需要先切换到您的本地目标分支，然后执行推送命令。</p><h3 id="推送所有分支" tabindex="-1"><a class="header-anchor" href="#推送所有分支" aria-hidden="true">#</a> 推送所有分支</h3><p>例如：推送所有本地分支到远程仓库。如果远程仓库中不存在相应的分支，它们也会被创建。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">--all</span> origin
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="推送标签" tabindex="-1"><a class="header-anchor" href="#推送标签" aria-hidden="true">#</a> 推送标签</h3><p>例如：将特定的标签推送到远程仓库。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push origin <span class="token operator">&lt;</span>tag-name<span class="token operator">&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="推送所有标签" tabindex="-1"><a class="header-anchor" href="#推送所有标签" aria-hidden="true">#</a> 推送所有标签</h3><p>例如：将所有本地标签推送到远程仓库。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">--tags</span> origin
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="同步本地仓库和远程仓库" tabindex="-1"><a class="header-anchor" href="#同步本地仓库和远程仓库" aria-hidden="true">#</a> 同步本地仓库和远程仓库</h3><p>删除在远程仓库中不存在但本地仍然引用的远程分支</p><p>例如：如果本地的同名分支不存在了，那么远程分支 <code>tmp</code> 将被删除。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">--prune</span> remote refs/heads/*:refs/tmp/*
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>将确保如果 <code>refs/heads/foo</code> 不存在，那么远程的 <code>refs/tmp/foo</code> 将被删除。</p><h3 id="删除远程分支" tabindex="-1"><a class="header-anchor" href="#删除远程分支" aria-hidden="true">#</a> 删除远程分支</h3><p>例如：删除远程 <code>test</code> 分支。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push origin <span class="token parameter variable">-d</span> <span class="token builtin class-name">test</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>只会删除远程分支，本地分支依然存在。</p><h3 id="强制推送" tabindex="-1"><a class="header-anchor" href="#强制推送" aria-hidden="true">#</a> 强制推送</h3><p>有时您可能需要覆盖远程仓库上的更改。这通常是不推荐的，因为它可能会丢失其他人的工作，但如果您确定要这样做，可以使用 <code>-f</code> 或 <code>--force</code> 选项。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">-f</span> origin main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>或者更安全地，您可以使用 <code>--force-with-lease</code>，它会检查远程分支是否包含您的本地更改，从而避免覆盖其他人的工作。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push --force-with-lease origin main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="推送并设置上游分支" tabindex="-1"><a class="header-anchor" href="#推送并设置上游分支" aria-hidden="true">#</a> 推送并设置上游分支</h3><p>当第一次推送一个本地分支到远程仓库时，可能想要设置该远程分支作为此本地分支的上游（<code>upstream</code>）。这样，将来使用 <code>git pull</code> 和 <code>git push</code> 而不必指定远程仓库和分支名称。</p><p>例如：使用 <code>-u</code> 选项指定该远程分支作为此本地分支的上游。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">-u</span> origin <span class="token builtin class-name">test</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="显示将要推送的远程分支" tabindex="-1"><a class="header-anchor" href="#显示将要推送的远程分支" aria-hidden="true">#</a> 显示将要推送的远程分支</h3><p>例如：使用 <code>-n</code> 查看将要推送的远程分支，但不实际推送。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">-n</span> origin
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="推送到远程指定分支" tabindex="-1"><a class="header-anchor" href="#推送到远程指定分支" aria-hidden="true">#</a> 推送到远程指定分支</h3><p>推送当前分支到远程引用匹配 <code>master</code> 的 <code>origin</code> 仓库中。这种形式可以方便地推送当前分支，而不必考虑其本地名称。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push origin HEAD:master
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,47),i=[n];function r(c,l){return a(),s("div",null,i)}const p=e(d,[["render",r],["__file","git_push.html.vue"]]);export{p as default};
