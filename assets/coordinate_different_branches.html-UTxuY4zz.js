import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as n,c as a,f as s}from"./app-3Se_7kLi.js";const i={},l=s(`<h2 id="问题" tabindex="-1"><a class="header-anchor" href="#问题" aria-hidden="true">#</a> 问题</h2><p>在使用 <code>git pull</code> 命令时，出现以下错误：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> pull

hint: You have divergent branches and need to specify how to reconcile them.
hint: You can <span class="token keyword">do</span> so by running one of the following commands sometime before
hint: your next pull:
hint:
hint:   <span class="token function">git</span> config pull.rebase <span class="token boolean">false</span>  <span class="token comment"># merge</span>
hint:   <span class="token function">git</span> config pull.rebase <span class="token boolean">true</span>   <span class="token comment"># rebase</span>
hint:   <span class="token function">git</span> config pull.ff only       <span class="token comment"># fast-forward only</span>
hint:
hint: You can replace <span class="token string">&quot;git config&quot;</span> with <span class="token string">&quot;git config --global&quot;</span> to <span class="token builtin class-name">set</span> a default
hint: preference <span class="token keyword">for</span> all repositories. You can also pass --rebase, --no-rebase,
hint: or --ff-only on the <span class="token builtin class-name">command</span> line to override the configured default per
hint: invocation.
fatal: Need to specify how to reconcile divergent branches.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="原因" tabindex="-1"><a class="header-anchor" href="#原因" aria-hidden="true">#</a> 原因</h2><p>这个提示是 Git 给出的，旨在帮助您决定如何处理分叉的分支，在您下一次执行 <code>git pull</code> 操作时如何合并这些更改。当本地分支和远程分支有不同的提交历史时，就会出现分叉，这通常发生在别人已经推送（<code>push</code>）了一些提交到远程仓库，而您的本地仓库还没有这些更新。</p><h2 id="解决方法" tabindex="-1"><a class="header-anchor" href="#解决方法" aria-hidden="true">#</a> 解决方法</h2><h3 id="解决方法-1" tabindex="-1"><a class="header-anchor" href="#解决方法-1" aria-hidden="true">#</a> 解决方法 1</h3><p>配置在拉取（<code>pull</code>）时合并（<code>merge</code>）远程分支的更改。如果有冲突，需要手动解决，这是默认策略。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config pull.rebase <span class="token boolean">false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>执行以上命令后，再次执行 <code>git pull</code> 命令。</p><h3 id="解决方法-2" tabindex="-1"><a class="header-anchor" href="#解决方法-2" aria-hidden="true">#</a> 解决方法 2</h3><p>配置在拉取（<code>pull</code>）远程分支之前，先将您本地的更改变基（<code>rebase</code>）到远程分支的顶部。这样可以保持项目历史的线性。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config pull.rebase <span class="token boolean">true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>执行以上命令后，再次执行 <code>git pull</code> 命令。</p><h3 id="解决方法-3" tabindex="-1"><a class="header-anchor" href="#解决方法-3" aria-hidden="true">#</a> 解决方法 3</h3><p>配置只允许快进（Fast-forward）合并。如果 <code>git pull</code> 操作不能通过快进完成（即需要合并或变基来解决分叉），这个命令会导致失败。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config pull.ff only
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>执行以上命令后，再次执行 <code>git pull</code> 命令。</p>`,18),o=[l];function d(c,t){return n(),a("div",null,o)}const u=e(i,[["render",d],["__file","coordinate_different_branches.html.vue"]]);export{u as default};
